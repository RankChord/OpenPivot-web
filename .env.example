VITE_OPENPIVOT_PROXY_TARGET=http://10.0.0.214:3000
# Leave VITE_OPENPIVOT_API_URL empty for same-origin /v1 through a dev/prod reverse proxy.
# Set it only when the backend explicitly allows browser CORS.
VITE_OPENPIVOT_API_URL=
